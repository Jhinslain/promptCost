# Envoi des feedbacks (page /feedback) via Resend.
# Copie ce fichier en .env.local et renseigne la clé.
RESEND_API_KEY=

# Optionnel : surcharge des adresses (valeurs par défaut dans app/api/feedback/route.ts).
# FEEDBACK_TO=contact@ghis.fr
# FEEDBACK_FROM=HowManyPrompts <feedback@ghis.fr>
